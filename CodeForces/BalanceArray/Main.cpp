#include <iostream>
using namespace std;

int main(){
	cin.tie(0);
        
    // n > 0 && n % 2 == 0
    // Construct an array a where 
    // n/2 elements of a are even
    // n/2 right element of a are odd
    // all elements are positive
    
    int t; cin >> t;
    
    while(t--){
        int n; cin >> n;

    }

	return 0;
}
